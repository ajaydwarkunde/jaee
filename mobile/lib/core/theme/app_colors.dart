import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand colors
  static const Color cream = Color(0xFFFBF6F3);
  static const Color blush = Color(0xFFF2E3E8);
  static const Color champagne = Color(0xFFE4D5CF);
  static const Color charcoal = Color(0xFF2D2D2D);
  static const Color warmGray = Color(0xFF6B6B6B);
  static const Color softWhite = Color(0xFFFFFFFF);

  // Rose palette
  static const Color rose = Color(0xFF923C5B);
  static const Color roseLight = Color(0xFFB4617B);
  static const Color roseDark = Color(0xFF6E2D44);

  // Semantic colors
  static const Color success = Color(0xFF6B9E76);
  static const Color error = Color(0xFFC44D4D);
  static const Color warning = Color(0xFFD4A843);

  // Surface colors
  static const Color surface = softWhite;
  static const Color background = cream;
  static const Color cardBackground = softWhite;
  static const Color divider = Color(0xFFE8E0DC);

  // Text colors
  static const Color textPrimary = charcoal;
  static const Color textSecondary = warmGray;
  static const Color textOnRose = softWhite;
}
