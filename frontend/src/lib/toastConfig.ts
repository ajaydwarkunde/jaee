/** Visible toast lifetime — shorter than library default so banners clear quickly. */
export const DEFAULT_TOAST_DURATION_MS = 2200

/** Time before DOM removal after dismiss — library default is 1000ms which feels sluggish. */
export const DEFAULT_TOAST_REMOVE_DELAY_MS = 220
